<h1>Edit user</h1>
<%= form_for(@user) do |f| %>
	<%= render 'fields', :f => f %>
	<div class="actions">
		<%= f.submit "Update" %><br />
	</div>
<% end %>

<div id="gravatar_div">
	<% gravatar_for @user %>
	<a href="http://gravatar.com/emails" target="_blank">change avatar</a>
</div>