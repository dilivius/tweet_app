<h1 id="all_users">All users</h1>
<%= will_paginate %>

<ul class="users">
	<%# @users.each do |user| %>
		<%#= render 'user', :user => user %>
		<%#= render user %> 
	<%# end %>
	<%= render @users %>
</ul>

<%= will_paginate %>